# SKIP_KEYSTATIC controls whether the Keystatic admin UI (/keystatic) is mounted.
# The `build` script sets this to true so production builds exclude admin routes.
# Leave empty for `npm run dev` so /keystatic stays available locally.
SKIP_KEYSTATIC=
